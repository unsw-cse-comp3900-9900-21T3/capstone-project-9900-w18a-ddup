import React, { memo } from "react";

import { OrderWrapper } from "./style";

function Order() {

    return (
        <OrderWrapper>
            order
        </OrderWrapper>
    )
}

export default memo(Order)
