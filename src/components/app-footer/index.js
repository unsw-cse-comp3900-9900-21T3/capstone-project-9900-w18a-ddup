import React, { memo } from "react";

import {
    AppFooterWrapper
} from "./style"

function AppFooter() {
    return (
        <AppFooterWrapper>
            AppFooter...
        </AppFooterWrapper>
    )
}

export default memo(AppFooter)
